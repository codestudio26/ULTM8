# apps/student

Placeholder. React Native Student app — one codebase, distributed as the shared ULTM8
app plus optional per-School white-label rebuilds (Spec 55 §5, ultm8-app-publishing).

Not implemented in Phase 1. Real React Native initialization needs a Metro/Xcode/
Android toolchain not available in this scaffolding pass — this directory exists so the
monorepo layout matches ultm8-nestjs-module §6 from day one.
