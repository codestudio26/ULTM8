# apps/platform-admin

Placeholder. Internal-only web app: translations CMS, platform subscription plans,
cross-tenant management (Spec 55 §4.2, ultm8-nestjs-module §6). Must be a separately
deployed app with its own subdomain and identity realm from day one — never merged into
apps/school-portal (ultm8-tenant-isolation §3/§6).

Not implemented in Phase 1. This directory exists so the monorepo layout matches
ultm8-nestjs-module §6 from day one.
