{
  "name": "@ultm8/platform-admin",
  "version": "0.1.0",
  "private": true,
  "description": "React — Platform Admin (Spec §4). A genuinely separate app/deployment/identity realm from apps/school-portal — Slice 1 (Phase 32) shipped Cognito-backed sign-in and the AdminUser roster (invite/list/revoke); Slice 2 (Phase 33) added look-up-by-id screens for School, Franchise, and each one's PaymentAccount configuration. Not deployed anywhere yet — see README.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.102.8",
    "@ultm8/api-client": "*",
    "@ultm8/auth": "*",
    "@ultm8/ui": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.18.3"
  },
  "devDependencies": {
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^5.2.0",
    "typescript": "^5.6.3",
    "vite": "^8.2.2"
  }
}
