{
  "name": "@ultm8/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed SDK generated from apps/api's live OpenAPI spec (ultm8-nestjs-module §3) — the one source of truth for request/response shapes, consumed by all three frontend apps.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "openapi-typescript ./openapi.json -o ./src/generated/schema.d.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "openapi-fetch": "^0.13.8"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "typescript": "^5.6.3"
  }
}
