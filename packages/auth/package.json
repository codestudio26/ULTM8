{
  "name": "@ultm8/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Shared token-handling utilities for the 3 frontend apps (NOT shared identity — Platform Admin's realm stays separate, Spec §4.4). See src/index.ts for the JWT-storage decision and its tradeoff reasoning — flagged for Architect/product-owner confirmation, not settled.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
